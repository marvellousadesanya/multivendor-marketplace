const ButtonWhite = ({ btnText }) => {
  return (
    <div className="w-[180px] text-center py-2 bg-[#fff] font-semibold font-innerbody rounded text-mainColor">
      {btnText}
    </div>
  );
};

export default ButtonWhite;
