const Button = ({ btnText }) => {
  return (
    <div className="w-[220px] text-center py-[5px] bg-mainColor font-semibold font-innerbody rounded text-[#fff]">
      {btnText}
    </div>
  );
};

export default Button;
