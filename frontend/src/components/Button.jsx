const Button = ({ btnText }) => {
  return (
    <div className="w-[220px] text-center py-[5px] bg-[#F77F00] font-semibold font-innerbody rounded text-[#fff]">
      {btnText}
    </div>
  );
};

export default Button;
